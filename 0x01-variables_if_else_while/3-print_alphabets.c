#include <stdio.h>
/**
 * main - entry point
 * Return: 0
 */

int main(void)
{
	char b;

	char p;

	b = 'a';
	p = 'A';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	while
		(p <= 'Z') {
			putchar(p);
			p++;
		}
	putchar('\n');
	return (0);
}
