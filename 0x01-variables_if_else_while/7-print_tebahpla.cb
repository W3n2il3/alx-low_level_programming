#include <stdio.h>

/**
 * main - the program that print alphabet in a lower case
 * Return: 0
 */

int main(void)
{
        char alphabet = 'z';

        while (alphabet <= 'a')
        {
                alphabet++;
        }
        putchar('\n');
        return (0);
