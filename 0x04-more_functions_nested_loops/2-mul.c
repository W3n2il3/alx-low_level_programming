#include "main.h"

/**
 *mul -multiples wo integers
 *@x: the first number to be multiply
 *@y: the second number to be multiply by the first number
 *Return: the result of multiplication of x and y paran
 */

int mul(int x, int y)
{
	return (x * y);

}
